#   pip install -r requirements.txt

# --- db.py ---
rapidfuzz>=3.14.5          # fuzzy company-name dedup (token_set_ratio)

# --- search_tool.py ---
ddgs>=9.14.4                # DuckDuckGo search, the renamed duckduckgo_search
tenacity>=9.1.4             # retry/backoff around DDG calls
langchain-core>=1.4.8       # @tool decorator for search_tech_companies

# --- crawler.py ---
requests>=2.34.2            # contact-page fetching + robots.txt
beautifulsoup4>=4.13.5      # HTML parsing

# --- agents.py / crew_scout.py / crew_copywriter.py ---
crewai>=1.14.7              # Agent / Task / Crew / LLM
litellm>=1.89.3             # required for crewai's Groq provider support,
                             # crewai does not reach Groq without this installed
python-dotenv>=1.2.2        # loads GROQ_API_KEY from .env

# --- cli.py ---
typer>=0.25.1                # CLI commands: scout, enrich, leads, draft, track, stats

# --- tests/test_db.py ---
pytest>=9.1.1